<div class="section">
   <div class="container-right-aligned-top">
      <div class="img-container-right-aligned-top">
         <img src="{{ img_1_url }}" alt="{{ img_1_description }}" />
         <img src="{{ img_2_url }}" alt="{{ img_2_description }}" />
      </div>
   </div>
   <div class="brand-service-container">
      <div class="company">
         <p>{{ project_company }}</p>
      </div>
      <div class="line"></div>
      <div class="main-service">
         <p>{{ project_main_service }}</p>
      </div>
   </div>
</div>

<style>
.container-right-aligned-top {
  height: 100dvh;
  width: 100vw;
  display: flex;
  justify-content: end;
}

.img-container-right-aligned-top {
  height: 100dvh;
  width: 80vw;
  display: flex;
  overflow: hidden;
}

.img-container-right-aligned-top img {
  height: 100dvh;
  width: 50%;
  object-fit: cover;
  object-position: top; /* This ensures cropping from the bottom */
}

@media (max-width: 992px) and (orientation: portrait) {
  .container-right-aligned {
    height: calc(100dvh - 12rem) !important;
    width: 100vw;
    margin-top: 8.75rem !important;
    margin-bottom: 3.25rem;
    justify-content: start;
    align-items: center;
  }

  .img-container-right-aligned-top {
    height: fit-content;
    width: 100vw;
    display: flex;
    align-items: center;
  }

  .img-container-right-aligned-top img {
    height: auto;
    aspect-ratio: 4 / 5;
    width: 50%;
    object-fit: cover;
    object-position: top; /* This ensures cropping from the bottom */
  }
}
</style>